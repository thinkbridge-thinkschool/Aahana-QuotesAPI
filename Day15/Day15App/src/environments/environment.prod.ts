// Replace with the deployed Week-1 QuotesApi base URL (Container App FQDN
// or its custom domain) before running a production build. There is no
// secret here — this is a public API base URL, not a credential.
export const environment = {
  production: true,
  apiBaseUrl: 'https://quotes-api.orangebeach-4969d067.centralindia.azurecontainerapps.io'
};
